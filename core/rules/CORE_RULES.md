# Core Rules

1. Все сущности системы должны быть описаны в core/entities.
2. Все связи должны быть описаны в core/relations.
3. Ни одна сущность не существует без версии.
4. Все изменения проходят через versioning.
5. ChatGPT не хранит данные — только генерирует их.